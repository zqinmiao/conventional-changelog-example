{
  "name": "conventional-changelog-example",
  "version": "1.0.12",
  "description": "conventional-changelog 生态探索",
  "main": "index.js",
  "scripts": {
    "version": "conventional-changelog -p angular -n './changelog-config.js' -c './changelog-context.json' -i CHANGELOG.md -s && git add CHANGELOG.md"
  },
  "keywords": [
    "conventional-changelog"
  ],
  "author": "reahink",
  "license": "ISC",
  "devDependencies": {
    "conventional-changelog-cli": "^2.1.1"
  }
}
